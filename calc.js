const numberBtns = document.querySelectorAll('.button');
const operatorBtns = document.querySelectorAll('.button I');
const clearBtn = document.querySelector('.button c');

numberBtns.forEach(button => {
    button.addEventListener('click', () => {
        calculator
    })
})